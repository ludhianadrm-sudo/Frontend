/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  env:{
    url:"https://backend-three-kohl-11.vercel.app/"
  },

  reactCompiler: true,
};

export default nextConfig;
